<%= form_for(@my_controller) do |f| %>
  <% if @my_controller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_controller.errors.count, "error") %> prohibited this my_controller from being saved:</h2>

      <ul>
      <% @my_controller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
